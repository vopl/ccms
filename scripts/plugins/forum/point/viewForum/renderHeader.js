let target = request.planData.forum;let parentForum = target.parent;//global.cache.fire('forum.forum');let t = arguments.callee.t;if(!t){	t = router.createTemplate();	t.compile(<>		<span>			{t.logo}		</span>		<span>			<strong>{t.title}</strong>		</span>	</>);	arguments.callee.t = t;}t = t.clone();if(target.image_id){	let img = global.cache.process({		key:'forum.'+target.id+'.image',		provider:function() orm.Image.load({id:target.image_id}),		events:['forum.forum', 'forum.'+target.id],	});	t.logo = ui.skin.tag(		'img', 		undefined,		{			src:'/filestore/'+img.location_thumb, 			width:img.width_thumb,			height:img.height_thumb,			});}t.title = target.title;return t;let manageForum;if(parentForum){	manageForum = <>		<a href={target.path+'/edit?backUrl='+parentForum.path+'/'+target.id}> edit </a>		<a href={target.path+'/del?backUrl='+parentForum.path}> del </a>		<a href={parentForum.path}> parent </a>		<a href={target.path+'/add?backUrl='+target.path}> add </a>	</>;}else{	manageForum = <>		<a href={target.path+'/edit?backUrl='+request.path}> edit </a>		<a href={target.path+'/add?backUrl='+target.path}> add </a>	</>;}return 	<><h2>{target.map_title}</h2><div>{target.description}</div>{manageForum}</>