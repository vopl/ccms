let res = [];let delim = <b>::</b>;for(let i = 0; i<request.plan.length; i++){	let p = request.plan[i];		let title = p.title;	if(!title && p.point.properties.title) title = p.point.properties.title();	if(!title) title = '';		if(i)	{		res.push(delim);	}	res.push(ui.skin.link(p.path, title));}return res;